# Структура проекта Fin Tracker

## 📁 Общая структура

```
fin-tracker/
├── src/                    # Исходный код приложения
├── public/                 # Статические файлы (HTML, иконки, manifest)
├── ios/                    # iOS проект для Capacitor
├── build/                  # Собранное приложение (генерируется автоматически)
├── node_modules/          # Зависимости (генерируется автоматически)
└── Конфиги                 # Настройки проекта
```

---

## 📂 src/ - Исходный код

### 🎯 `src/index.tsx` - Точка входа
**Что делает:** Запускает React приложение, рендерит `<App />` в DOM
**Зачем:** Это первая точка входа, которая выполняется при загрузке страницы

```typescript
// Здесь React "монтирует" приложение в элемент #root
ReactDOM.createRoot(document.getElementById('root')).render(<App />)
```

---

### 🎯 `src/App.tsx` - Главный компонент приложения
**Что делает:** 
- Управляет навигацией между вкладками (Dashboard, Accounts, Recurring, Goals)
- Оборачивает приложение в `FinanceDataProvider`
- Отображает Header и Bottom Navigation

**Структура:**
- `activeTab` - текущая активная вкладка
- `tabs` - массив вкладок с компонентами
- `ActiveComponent` - компонент, который отображается сейчас

**Зачем:** Это "скелет" приложения, который определяет общую структуру и навигацию

---

## 📂 `src/app/` - Конфигурация приложения

### `app/components/` - Компоненты страниц
**Что это:** Основные экраны приложения (страницы)

- `Dashboard.tsx` - Главная страница с балансом, графиком и транзакциями
- `Accounts.tsx` - Управление счетами
- `Recurring.tsx` - Периодические платежи
- `Goals.tsx` - Цели накопления
- `Transactions.tsx` - Отдельная страница транзакций (не используется в навигации)

**Зачем:** Каждый файл = одна страница/экран приложения

---

### `app/pages/` - Страницы (устаревшее)
**Что это:** Старые страницы, которые не используются
- `TestPage.tsx` - тестовая страница (можно удалить)

**Зачем:** Раньше планировалось использовать роутинг, но сейчас используется таб-навигация

---

### `app/providers/` - Context Providers
**Что это:** Провайдеры для глобального состояния

- `DatabaseProvider.tsx` - Провайдер для работы с IndexedDB
  - Инициализирует базу данных
  - Предоставляет `useDatabase()` хук
  
- `FinanceDataProvider.tsx` - Главный провайдер финансовых данных
  - Загружает данные из БД (accounts, transactions, goals, recurring)
  - Предоставляет функции для добавления/обновления данных
  - Предоставляет `useFinanceData()` хук
  
- `AppProvider.tsx` - Обертка (не используется, можно удалить)

**Зачем:** Context API позволяет делиться данными между компонентами без prop drilling

---

## 📂 `src/features/` - Доменные фичи (feature-first подход)

**Что это:** Структура для будущего расширения функционала

**Идея:** Каждая фича изолирована и содержит:
- `components/` - компоненты фичи
- `hooks/` - хуки для работы с данными фичи
- `services/` - бизнес-логика
- `types/` - типы TypeScript

**Текущее состояние:** Папки пустые, так как вся логика сейчас в `app/components/`

**Зачем:** Когда приложение вырастет, можно будет вынести логику в фичи для лучшей организации

**Пример использования в будущем:**
```
features/accounts/
  ├── components/AccountList.tsx
  ├── hooks/useAccounts.ts
  ├── services/accountService.ts
  └── types/account.types.ts
```

---

## 📂 `src/shared/` - Общий переиспользуемый код

### `shared/components/` - UI компоненты

**`ui/`** - Компоненты из shadcn/ui:
- `button.tsx` - кнопки
- `card.tsx` - карточки
- `dialog.tsx` - модальные окна
- `input.tsx` - поля ввода
- `select.tsx` - выпадающие списки
- `badge.tsx` - бейджи
- `progress.tsx` - прогресс-бары
- `label.tsx` - метки для форм
- `calendar.tsx` - календарь
- `checkbox.tsx` - чекбоксы

**Зачем:** Готовые переиспользуемые компоненты с единым стилем

**`layout/`** - Компоненты макета:
- `Footer.tsx` - футер (не используется, можно удалить)

---

### `shared/utils/` - Утилиты

- `id/idUtils.ts` - генерация уникальных ID
- `currency/currencyUtils.ts` - работа с валютами
- `date/dateUtils.ts` - работа с датами
- `validation/validationUtils.ts` - валидация данных
- `performance/` - пустая папка (можно удалить)

**Зачем:** Переиспользуемые функции для работы с данными

---

### `shared/types/` - Общие типы TypeScript

- `common.types.ts` - базовые типы (BaseEntity)
- `database.types.ts` - типы для работы с БД (устаревшие, можно удалить)

**Зачем:** Централизованные типы для использования по всему проекту

---

### `shared/lib/` - Библиотеки

- `utils.ts` - функция `cn()` для объединения CSS классов (Tailwind)

**Зачем:** Утилиты для работы с библиотеками

---

### `shared/constants/` - Константы
**Пустая папка** - можно удалить или использовать для констант (цвета, размеры и т.д.)

---

### `shared/hooks/` - Общие хуки
**Пустая папка** - можно удалить или использовать для общих хуков

---

## 📂 `src/services/` - Сервисы уровня приложения

### `services/database/`
- `db.ts` - Работа с IndexedDB через Dexie
  - Определяет структуру БД (таблицы)
  - Инициализирует БД
  - Заполняет предустановленными категориями

**Зачем:** Централизованная работа с базой данных

---

## 📂 `src/assets/` - Ресурсы

- `fonts/` - шрифты (пусто)
- `icons/` - иконки (пусто)
- `images/` - изображения (пусто)

**Зачем:** Статические ресурсы (иконки, изображения, шрифты)

---

## 📂 `src/styles/` - Стили
**Пустая папка** - можно удалить (стили в `index.css` и через Tailwind)

---

## 📄 Другие файлы в src/

- `App.css` - стили для App компонента (не используется, можно удалить)
- `index.css` - глобальные стили (Tailwind CSS)
- `logo.svg` - логотип React (не используется, можно удалить)
- `react-app-env.d.ts` - типы для Create React App (нужен)
- `App.test.tsx` - тест для App (можно оставить или удалить)
- `setupTests.ts` - настройка тестов (нужен для тестов)
- `reportWebVitals.ts` - метрики производительности (можно оставить)

---

## 🔧 Конфиги в корне проекта

### Обязательные:
- `package.json` - зависимости и скрипты
- `tsconfig.json` - настройки TypeScript
- `tailwind.config.js` - настройки Tailwind CSS
- `postcss.config.js` - настройки PostCSS
- `craco.config.js` - настройки CRACO (для переопределения CRA)
- `components.json` - настройки shadcn/ui
- `capacitor.config.ts` - настройки Capacitor (для iOS)

### Документация (можно оставить):
- `README.md` - описание проекта
- `PROMPT_FOR_AI.md` - промпт для AI
- `DEVELOPMENT_PLAN.md` - план разработки
- `TESTING_GUIDE.md` - руководство по тестированию
- `PWA_AND_STORAGE.md` - документация по PWA
- `PROJECT_STRUCTURE.md` - этот файл

---

## 🗑️ Удаленные файлы (уже почищено):

✅ **`src/app/pages/TestPage.tsx`** - тестовая страница (устаревшая)  
✅ **`src/app/providers/AppProvider.tsx`** - не используется  
✅ **`src/shared/components/layout/Footer.tsx`** - не используется  
✅ **`src/App.css`** - не используется (стили через Tailwind)  
✅ **`src/logo.svg`** - не используется  
✅ **`src/shared/types/database.types.ts`** - устаревшие типы  

**Пустые папки можно оставить** - они могут понадобиться в будущем для организации кода.

---

## 📊 Поток данных в приложении

```
index.tsx
  └── App.tsx
      └── FinanceDataProvider
          ├── DatabaseProvider (инициализирует IndexedDB)
          └── FinanceDataProviderInner
              ├── Загружает данные из IndexedDB
              ├── Предоставляет данные через Context
              └── Компоненты страниц (Dashboard, Accounts, etc.)
                  └── Используют useFinanceData() для доступа к данным
```

---

## 🎯 Как добавить новую страницу?

1. Создать компонент в `src/app/components/NewPage.tsx`
2. Добавить в `tabs` массив в `App.tsx`:
   ```typescript
   { id: 'newpage', label: 'New Page', icon: Icon, component: NewPage }
   ```

---

## 🎯 Как добавить новую фичу?

1. Создать папку в `src/features/new-feature/`
2. Создать структуру:
   - `components/` - компоненты фичи
   - `hooks/` - хуки
   - `services/` - бизнес-логика
   - `types/` - типы
3. Использовать в компонентах страниц

---

## 💡 Рекомендации

1. **Все данные** хранятся в IndexedDB через `FinanceDataProvider`
2. **UI компоненты** берутся из `shared/components/ui/`
3. **Утилиты** находятся в `shared/utils/`
4. **Типы** экспортируются из `FinanceDataProvider` или `shared/types/`
5. **Стили** через Tailwind CSS (классы в JSX)

