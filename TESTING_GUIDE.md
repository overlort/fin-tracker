# Руководство по тестированию приложения

## 🚀 Способы запуска

### Вариант 1: Обычный dev server (для проверки UI)

SQLite **не будет работать** в обычном браузере, но вы сможете проверить UI и увидеть сообщение об ошибке.

```bash
npm start
```

Приложение откроется на `http://localhost:3000`

**Что вы увидите:**
- ✅ UI приложения загрузится
- ❌ Сообщение об ошибке: "SQLite работает только в Capacitor окружении"
- ℹ️ Информация о платформе покажет, что Capacitor не активен

### Вариант 2: Capacitor dev server (для работы БД)

Для тестирования SQLite нужно использовать Capacitor dev server:

```bash
# 1. Соберите проект
npm run build

# 2. Синхронизируйте с Capacitor
npx cap sync ios

# 3. Запустите Capacitor dev server
npx cap run ios
```

Или для веб-версии с плагинами:

```bash
# Установите Capacitor CLI глобально (если еще не установлен)
npm install -g @capacitor/cli

# Запустите dev server с плагинами
npx cap serve
```

### Вариант 3: iOS симулятор/устройство

Для полного тестирования на iOS:

```bash
# 1. Соберите проект
npm run build

# 2. Синхронизируйте
npx cap sync ios

# 3. Откройте в Xcode
npx cap open ios

# 4. Запустите из Xcode на симуляторе или устройстве
```

## 📱 Что тестировать

### Тестовая страница (`/`)

На тестовой странице вы увидите:

1. **Статус БД**
   - ✅ "База данных успешно инициализирована" - если все работает
   - ❌ Сообщение об ошибке - если что-то пошло не так
   - ⏳ "Инициализация базы данных..." - во время загрузки

2. **Список категорий**
   - После успешной инициализации должны появиться 8 предустановленных категорий:
     - 🍔 Продукты
     - 🚗 Транспорт
     - 🎬 Развлечения
     - 💊 Здоровье
     - 👕 Одежда
     - 🏠 Жилье
     - 📚 Образование
     - 📦 Прочее

3. **Информация о платформе**
   - Показывает, запущено ли приложение в Capacitor
   - Показывает текущую платформу (ios, android, web)

## 🔍 Проверка работы БД

### Что должно произойти при инициализации:

1. ✅ Создание/открытие БД `fin_tracker.db`
2. ✅ Создание таблицы миграций
3. ✅ Применение миграции 001 (создание всех таблиц)
4. ✅ Заполнение предустановленными категориями

### Как проверить в консоли:

Откройте DevTools (F12) и проверьте консоль:

- ✅ Нет ошибок - все хорошо
- ❌ Ошибки инициализации - проверьте логи

### Типичные проблемы:

1. **"SQLite работает только в Capacitor окружении"**
   - Решение: Используйте `npx cap serve` или запустите на мобильном устройстве

2. **Ошибка при инициализации БД**
   - Проверьте, что `@capacitor-community/sqlite` установлен
   - Проверьте консоль на детали ошибки

3. **Категории не загружаются**
   - Проверьте, что инициализация прошла успешно
   - Нажмите кнопку "Обновить" на странице

## 🛠️ Отладка

### Просмотр логов:

```bash
# В терминале при запуске npm start
# Или в консоли браузера (F12)
```

### Проверка структуры БД:

В коде можно добавить временный запрос для проверки таблиц:

```typescript
const result = await db.query(
  "SELECT name FROM sqlite_master WHERE type='table'"
);
console.log('Tables:', result.values);
```

## 📝 Следующие шаги

После успешного тестирования БД:

1. ✅ Убедитесь, что категории загружаются
2. ✅ Проверьте, что нет ошибок в консоли
3. ✅ Убедитесь, что UI отображается корректно
4. 🔄 Можно переходить к реализации фич (категории, расходы и т.д.)

## ⚠️ Важно

- SQLite через Capacitor **не работает** в обычном браузере (localhost:3000)
- Для тестирования БД используйте Capacitor dev server или мобильное приложение
- UI можно тестировать в обычном браузере, но БД будет недоступна

